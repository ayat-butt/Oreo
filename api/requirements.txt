# COCO Contracts web backend (FastAPI on Railway)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# Data
psycopg2-binary>=2.9          # Markaz (read-only) + app DB
SQLAlchemy>=2.0
alembic>=1.13

# Google + AI (engine reuse)
google-api-python-client>=2.0.0
google-auth>=2.0
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.2.0
anthropic>=0.40.0

# Auth / web
authlib>=1.3                  # Google SSO (OAuth2 authorization code)
PyJWT>=2.8                    # app session JWT
tenacity>=8.2                 # Google API backoff
python-dotenv>=1.0
sentry-sdk[fastapi]>=2.0      # error tracking (optional)
